# Cloudflare Pages — MF SMC Trader
# Docs: https://developers.cloudflare.com/pages/

name = "forex-insights-terminal"
pages_build_output_dir = ".output/public"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

# ── Build settings (used by Cloudflare Pages CI) ─────────────────────────────

# ── Environment variables (non-secret) ────────────────────────────────────────
# Secrets are set via the Cloudflare Dashboard → Settings → Environment Variables
# (mark them as "Secret" so they are encrypted):
#   TWELVEDATA_API_KEY
#   SESSION_SECRET
#   SUPABASE_URL
#   SUPABASE_PUBLISHABLE_KEY
#   SUPABASE_PROJECT_ID
#   VITE_SUPABASE_URL
#   VITE_SUPABASE_PUBLISHABLE_KEY
#   VITE_SUPABASE_PROJECT_ID
